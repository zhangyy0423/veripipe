// 仅用于验证 framework 的 product-neutral adapter contract，不代表真实产品 readiness。
import { expect, test } from '@playwright/test';

test('sample product contract fixture', async () => {
  expect(true).toBe(true);
});
